-----------------------------------------
  for (List)
-----------------------------------------
#for(String mon: months)
    ${for.index}: ${mon}
#end

-----------------------------------------
  for (Map)
-----------------------------------------
#for(Map.Entry entry: user)
    ${entry.key}: ${entry.value}
#end

-----------------------------------------
  count-loop
-----------------------------------------
#for(int i: range(0,10))${i},#end
#for(int i: range(10,0,-2))${i},#end

-----------------------------------------
  for / else
-----------------------------------------
#for(Object item: emptyItems)
    ${item}
#else
    No items
#end
